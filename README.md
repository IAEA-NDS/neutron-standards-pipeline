# Neutron Standards Pipeline

Preliminary evaluation pipeline for the preparation the neutron data standards.
This pipeline performs:

1) Data reduction with `datpy`
2) Cross section evaluation with `gmapy` 
3) Using the `gmapy` result to update prior values used for data reduction


**This is work in progress**
